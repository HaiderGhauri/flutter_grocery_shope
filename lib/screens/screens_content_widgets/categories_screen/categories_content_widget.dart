import 'package:flutter/material.dart';

class CategoriesContentWidget extends StatelessWidget {
  const CategoriesContentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("categories Screen");
  }
}